There are <%= locals[:rows].count %> rows.<br>
There should be another row every time you referesh.<br>
If that's the case then everything works!!
<br><br>
Here's an image in the `public` folder.<br>
<img src="/image.jpg" alt="test image">